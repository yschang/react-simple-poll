{
  "name": "react-simple-poll",
  "version": "1.0.2",
  "description": "A simple poll component",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && NODE_ENV=production babel src/lib --out-dir dist --copy-files",
    "start": "react-scripts start"
  },
  "author": "yschang",
  "license": "MIT",
  "peerDependencies": {
    "randomcolor": "^0.6.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.18.10",
    "@babel/core": "^7.18.13",
    "@babel/preset-env": "^7.18.10",
    "@babel/preset-react": "^7.18.6"
  },
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yschang/react-simple-poll.git"
  },
  "dependencies": {
    "@babel/polyfill": "^7.12.1",
    "react-scripts": "5.0.1",
    "react-dom": "^18.2.0",
    "web-vitals": "^2.1.4"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
